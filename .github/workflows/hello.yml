name: "hello world"

# Controls when the action will run. Triggers the workflow on push done to the master or production branch
on:
  push:
    branches:
      - master
      - production


jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: "print hellow world"
              run: echo "Hello World"



        